name: make-master
on: 
  push:
    branches: 
      - master
      - develop
      - feature/*
      - bugfix/*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: 1
      - name: Run Makefile
        env:
          DOCKER_USER: kris9819
          DOCKER_PASSWORD: super_secret_password
        run: make all
